name: flexy_wrap
description: A powerful, flexible wrap layout for Flutter that intelligently balances rows and proportionally squishes or expands its children.
version: 0.0.1
homepage: https://github.com/Frogperson/flexy_wrap

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
